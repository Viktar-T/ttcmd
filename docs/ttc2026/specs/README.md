# specs/

One folder per slice: `NNN-slug/` with `spec.md`, `plan.md`, `tasks.md`.
Numbered, append-only; a later slice supersedes, never rewrites. The queue
lives in `roadmap.md`; the loop and the two modes in `AGENTS.md` §2.
Empty until slice 001 runs.
